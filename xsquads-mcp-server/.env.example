# Path to the root of your xsquads repository.
# The server will scan all subdirectories for squad definitions.
# If not set, defaults to the parent directory of the mcp-server folder.
XSQUADS_ROOT=/path/to/your/xsquads
